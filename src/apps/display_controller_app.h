#pragma once

// The indoor e-paper display node. Renders the 3-page UI (main / detail /
// actuators) from the data model and handles button + rotary navigation.
void display_controller_setup();
void display_controller_loop();
